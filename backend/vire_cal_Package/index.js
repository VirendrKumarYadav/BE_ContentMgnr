console.log("Starting");

const sum = (a, b) => a + b;
const minus = (a, b) => a + b;
const multiply = (a, b) =>  a + b;
const divide = (a, b) => a + b;

const data={sum, multiply, divide,minus}
module.export = data;